{
  "name": "vm-management",
  "version": "1.0.0",
  "description": "Frontend для системы мониторинга VM",
  "scripts": {
    "start": "live-server --port=3000 --open=/frontend/login.html",
    "dev": "live-server --port=3000 --open=/frontend/login.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}